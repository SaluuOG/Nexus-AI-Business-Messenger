import { safeTextSend } from '../drafts/safeTextSend';
import type { RealtimeChannel } from '@supabase/supabase-js';
import { supabase } from '../../lib/supabase';
import { createTextClientRequestId } from '../drafts/textSendRetry';
import type { MessageCursor } from './messageSearchData';

export type { MessageCursor } from './messageSearchData';

const ATTACHMENT_BUCKET = 'nexus-chat-attachments';
export const MAX_CHAT_ATTACHMENT_BYTES = 25 * 1024 * 1024;

export const SUPPORTED_CHAT_ATTACHMENT_TYPES = [
  'image/jpeg','image/png','image/webp','image/gif','image/heic','image/heif',
  'application/pdf','text/plain','text/csv','application/msword',
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
  'application/vnd.ms-excel','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
  'application/vnd.ms-powerpoint','application/vnd.openxmlformats-officedocument.presentationml.presentation',
  'application/zip','application/x-zip-compressed',
  'audio/webm','audio/ogg','audio/mp4','audio/mpeg','audio/wav','audio/x-m4a',
] as const;

const extensionMimeMap: Record<string, string> = {
  jpg:'image/jpeg',jpeg:'image/jpeg',png:'image/png',webp:'image/webp',gif:'image/gif',heic:'image/heic',heif:'image/heif',
  pdf:'application/pdf',txt:'text/plain',csv:'text/csv',doc:'application/msword',
  docx:'application/vnd.openxmlformats-officedocument.wordprocessingml.document',xls:'application/vnd.ms-excel',
  xlsx:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',ppt:'application/vnd.ms-powerpoint',
  pptx:'application/vnd.openxmlformats-officedocument.presentationml.presentation',zip:'application/zip',
  webm:'audio/webm',ogg:'audio/ogg',mp3:'audio/mpeg',wav:'audio/wav',m4a:'audio/x-m4a',mp4:'audio/mp4',
};

export type DirectConversation={conversation_id:string;contact_user_id:string;full_name:string|null;username:string|null;avatar_url:string|null;last_message:string|null;last_message_at:string|null;unread_count:number};
export type DirectAttachment={attachment_id:string;storage_path:string;file_name:string;mime_type:string;file_size:number;signed_url:string|null};
export type DirectMessage={message_id:string;sender_id:string;body:string;created_at:string;reply_to_message_id:string|null;reply_sender_id:string|null;reply_body:string|null;edited_at:string|null;deleted_at:string|null;read_at:string|null;attachments:DirectAttachment[]};
export type ContactPresence={contact_user_id:string;last_seen_at:string|null;online:boolean};
export type DirectMessagePage={messages:DirectMessage[];has_more:boolean;next_cursor:MessageCursor|null};
export type DirectMessageContext={messages:DirectMessage[];anchor_message_id:string;has_older:boolean;has_newer:boolean;oldest_cursor:MessageCursor|null;newest_cursor:MessageCursor|null};
export type MessageRealtimeChange={event:'INSERT'|'UPDATE'|null;messageId:string|null;scopeId:string|null};

function mimeTypeForFile(file:File){if(file.type)return file.type.toLowerCase();const ext=file.name.split('.').pop()?.toLowerCase()||'';return extensionMimeMap[ext]||''}
function extensionForFile(file:File){const ext=file.name.split('.').pop()?.toLowerCase().replace(/[^a-z0-9]/g,'')||'';return ext&&ext.length<=10?`.${ext}`:''}
function createObjectPath(conversationId:string,userId:string,file:File){const randomId=typeof crypto!=='undefined'&&'randomUUID'in crypto?crypto.randomUUID():`${Date.now()}-${Math.random().toString(36).slice(2)}`;return `${conversationId}/${userId}/${randomId}${extensionForFile(file)}`}
export function validateChatAttachment(file:File){const mimeType=mimeTypeForFile(file);if(!file.size)return{mimeType,error:'Die Datei ist leer.'};if(file.size>MAX_CHAT_ATTACHMENT_BYTES)return{mimeType,error:'Die Datei ist größer als 25 MB.'};if(!SUPPORTED_CHAT_ATTACHMENT_TYPES.includes(mimeType as (typeof SUPPORTED_CHAT_ATTACHMENT_TYPES)[number]))return{mimeType,error:'Dieser Dateityp wird noch nicht unterstützt.'};if(!file.name.trim()||file.name.trim().length>255)return{mimeType,error:'Der Dateiname ist ungültig.'};return{mimeType,error:null as string|null}}
async function signAttachments(messages:DirectMessage[]){if(!supabase)return messages;return Promise.all(messages.map(async message=>{const attachments=await Promise.all((message.attachments||[]).map(async attachment=>{const{data,error}=await supabase!.storage.from(ATTACHMENT_BUCKET).createSignedUrl(attachment.storage_path,3600);return{...attachment,file_size:Number(attachment.file_size||0),signed_url:error?null:data?.signedUrl??null}}));return{...message,attachments}}))}
function normalizeCursor(value:unknown):MessageCursor|null{if(!value||typeof value!=='object')return null;const cursor=value as Partial<MessageCursor>;return typeof cursor.created_at==='string'&&Number.isFinite(Date.parse(cursor.created_at))&&typeof cursor.message_id==='string'&&cursor.message_id.length>0?{created_at:cursor.created_at,message_id:cursor.message_id}:null}
function normalizeMessageRealtimeChange(value:unknown):MessageRealtimeChange{if(!value||typeof value!=='object')return{event:null,messageId:null,scopeId:null};const payload=value as{eventType?:unknown;new?:unknown};const event=payload.eventType==='INSERT'||payload.eventType==='UPDATE'?payload.eventType:null;const candidate=payload.new;if(!candidate||typeof candidate!=='object')return{event,messageId:null,scopeId:null};const row=candidate as{id?:unknown;conversation_id?:unknown};const id=row.id;const scopeId=row.conversation_id;return{event,messageId:typeof id==='string'&&id.length>0?id:null,scopeId:typeof scopeId==='string'&&scopeId.length>0?scopeId:null}}
function normalizeDirectMessages(value:unknown):DirectMessage[]|null{if(!Array.isArray(value))return null;const normalized:DirectMessage[]=[];for(const candidate of value){if(!candidate||typeof candidate!=='object')return null;const message=candidate as Partial<DirectMessage>;if(typeof message.message_id!=='string'||!message.message_id||typeof message.sender_id!=='string'||!message.sender_id||typeof message.body!=='string'||typeof message.created_at!=='string'||!Number.isFinite(Date.parse(message.created_at))||!Array.isArray(message.attachments))return null;const attachments:DirectAttachment[]=[];for(const item of message.attachments){if(!item||typeof item!=='object')return null;const attachment=item as Partial<DirectAttachment>&{file_size?:number|string};if(typeof attachment.file_size!=='number'&&typeof attachment.file_size!=='string')return null;const fileSize=Number(attachment.file_size);if(typeof attachment.attachment_id!=='string'||!attachment.attachment_id||typeof attachment.storage_path!=='string'||!attachment.storage_path||typeof attachment.file_name!=='string'||typeof attachment.mime_type!=='string'||!Number.isFinite(fileSize)||fileSize<1)return null;attachments.push({attachment_id:attachment.attachment_id,storage_path:attachment.storage_path,file_name:attachment.file_name,mime_type:attachment.mime_type,file_size:fileSize,signed_url:null})}normalized.push({message_id:message.message_id,sender_id:message.sender_id,body:message.body,created_at:message.created_at,reply_to_message_id:typeof message.reply_to_message_id==='string'?message.reply_to_message_id:null,reply_sender_id:typeof message.reply_sender_id==='string'?message.reply_sender_id:null,reply_body:typeof message.reply_body==='string'?message.reply_body:null,edited_at:typeof message.edited_at==='string'&&Number.isFinite(Date.parse(message.edited_at))?message.edited_at:null,deleted_at:typeof message.deleted_at==='string'&&Number.isFinite(Date.parse(message.deleted_at))?message.deleted_at:null,read_at:typeof message.read_at==='string'&&Number.isFinite(Date.parse(message.read_at))?message.read_at:null,attachments})}return normalized}
const emptyDirectMessagePage=():DirectMessagePage=>({messages:[],has_more:false,next_cursor:null});
function boundedInteger(value:number,fallback:number,maximum:number){return Number.isFinite(value)?Math.max(1,Math.min(Math.trunc(value),maximum)):fallback}
const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export async function openDirectConversation(contactUserId:string){if(!supabase)return{data:null as string|null,error:'Supabase ist nicht konfiguriert.'};const{data,error}=await supabase.rpc('open_direct_conversation',{p_contact_user_id:contactUserId});return{data:(data as string|null)??null,error:error?.message??null}}
export async function loadDirectConversations(){if(!supabase)return{data:[] as DirectConversation[],error:'Supabase ist nicht konfiguriert.'};const{data,error}=await supabase.rpc('get_direct_conversations');return{data:((data??[])as Array<Omit<DirectConversation,'unread_count'>&{unread_count:number|string}>).map(item=>({...item,unread_count:Number(item.unread_count||0)})),error:error?.message??null}}
export async function loadDirectMessages(conversationId:string){if(!supabase)return{data:[]as DirectMessage[],error:'Supabase ist nicht konfiguriert.'};const{data,error}=await supabase.rpc('get_direct_messages',{p_conversation_id:conversationId,p_limit:200});if(error)return{data:[]as DirectMessage[],error:error.message};const messages=normalizeDirectMessages(data);if(!messages)return{data:[]as DirectMessage[],error:'Die Nachrichten konnten nicht sicher geladen werden.'};return{data:await signAttachments(messages),error:null as string|null}}
export async function loadDirectMessagePage(conversationId:string,cursor:MessageCursor|null=null,limit=100){if(!supabase)return{data:emptyDirectMessagePage(),error:'Supabase ist nicht konfiguriert.'};const normalizedCursor=cursor===null?null:normalizeCursor(cursor);if(cursor&&!normalizedCursor)return{data:emptyDirectMessagePage(),error:'Der Nachrichten-Cursor ist ungültig.'};const{data,error}=await supabase.rpc('get_direct_message_page',{p_conversation_id:conversationId,p_before_created_at:normalizedCursor?.created_at??null,p_before_message_id:normalizedCursor?.message_id??null,p_limit:boundedInteger(limit,100,200)});if(error)return{data:emptyDirectMessagePage(),error:error.message};if(!data||typeof data!=='object')return{data:emptyDirectMessagePage(),error:'Die Nachrichten konnten nicht sicher geladen werden.'};const value=data as Partial<DirectMessagePage>;const messages=normalizeDirectMessages(value.messages);const nextCursor=value.next_cursor===null?null:normalizeCursor(value.next_cursor);if(!messages||typeof value.has_more!=='boolean'||(value.next_cursor!==null&&!nextCursor)||(messages.length>0&&!nextCursor)||(messages.length===0&&nextCursor!==null)||(value.has_more&&!nextCursor))return{data:emptyDirectMessagePage(),error:'Die Nachrichten konnten nicht sicher geladen werden.'};return{data:{messages:await signAttachments(messages),has_more:value.has_more,next_cursor:nextCursor},error:null as string|null}}
export async function loadDirectMessageContext(conversationId:string,messageId:string,radius=30){if(!supabase)return{data:null as DirectMessageContext|null,error:'Supabase ist nicht konfiguriert.'};const{data,error}=await supabase.rpc('get_direct_message_context',{p_conversation_id:conversationId,p_message_id:messageId,p_radius:boundedInteger(radius,30,50)});if(error||!data||typeof data!=='object')return{data:null as DirectMessageContext|null,error:error?.message??'Die Nachricht konnte nicht geladen werden.'};const value=data as Partial<DirectMessageContext>;const messages=normalizeDirectMessages(value.messages);const oldestCursor=value.oldest_cursor===null?null:normalizeCursor(value.oldest_cursor);const newestCursor=value.newest_cursor===null?null:normalizeCursor(value.newest_cursor);if(!messages||value.anchor_message_id!==messageId||!messages.some(message=>message.message_id===messageId)||typeof value.has_older!=='boolean'||typeof value.has_newer!=='boolean'||!oldestCursor||!newestCursor)return{data:null as DirectMessageContext|null,error:'Die Nachricht konnte nicht sicher geladen werden.'};return{data:{messages:await signAttachments(messages),anchor_message_id:messageId,has_older:value.has_older,has_newer:value.has_newer,oldest_cursor:oldestCursor,newest_cursor:newestCursor},error:null as string|null}}
export async function sendDirectMessage(conversationId:string,body:string,replyToMessageId?:string|null,clientRequestId?:string){if(!supabase)return{data:null as string|null,error:'Supabase ist nicht konfiguriert.'};const requestId=clientRequestId??createTextClientRequestId();if(!UUID_PATTERN.test(requestId))return{data:null as string|null,error:'Die Nachricht konnte nicht sicher gesendet werden.'};const{data,error}=await safeTextSend(() => supabase!.rpc('send_direct_message_v3',{p_conversation_id:conversationId,p_body:body,p_client_request_id:requestId,p_reply_to_message_id:replyToMessageId??null}));return{data:(data as string|null)??null,error:error?.message??null}}
export async function sendDirectAttachmentMessage(conversationId:string,currentUserId:string,file:File,body:string,replyToMessageId?:string|null){if(!supabase)return{data:null as string|null,error:'Supabase ist nicht konfiguriert.'};const validation=validateChatAttachment(file);if(validation.error)return{data:null as string|null,error:validation.error};const storagePath=createObjectPath(conversationId,currentUserId,file);const uploadResult=await supabase.storage.from(ATTACHMENT_BUCKET).upload(storagePath,file,{cacheControl:'3600',contentType:validation.mimeType,upsert:false});if(uploadResult.error)return{data:null as string|null,error:`Upload fehlgeschlagen: ${uploadResult.error.message}`};const{data,error}=await supabase.rpc('send_direct_attachment_message',{p_conversation_id:conversationId,p_storage_path:storagePath,p_file_name:file.name.trim(),p_mime_type:validation.mimeType,p_file_size:file.size,p_body:body.trim(),p_reply_to_message_id:replyToMessageId??null});if(error){await supabase.storage.from(ATTACHMENT_BUCKET).remove([storagePath]);return{data:null as string|null,error:error.message}}return{data:(data as string|null)??null,error:null as string|null}}
export async function editDirectMessage(messageId:string,body:string){if(!supabase)return{error:'Supabase ist nicht konfiguriert.'};const{error}=await supabase.rpc('edit_direct_message',{p_message_id:messageId,p_body:body});return{error:error?.message??null}}
export async function deleteDirectMessage(messageId:string,attachmentPaths:string[]=[]){if(!supabase)return{error:'Supabase ist nicht konfiguriert.'};if(attachmentPaths.length){const storageResult=await supabase.storage.from(ATTACHMENT_BUCKET).remove(attachmentPaths);if(storageResult.error)return{error:`Anhang konnte nicht gelöscht werden: ${storageResult.error.message}`}}const{error}=await supabase.rpc('delete_direct_message',{p_message_id:messageId});return{error:error?.message??null}}
export async function markDirectConversationRead(conversationId:string){if(!supabase)return{error:'Supabase ist nicht konfiguriert.'};const{error}=await supabase.rpc('mark_direct_conversation_read',{p_conversation_id:conversationId});return{error:error?.message??null}}
export async function touchUserPresence(){if(!supabase)return{error:'Supabase ist nicht konfiguriert.'};const{error}=await supabase.rpc('touch_user_presence');return{error:error?.message??null}}
export async function loadContactPresence(conversationId:string){if(!supabase)return{data:null as ContactPresence|null,error:'Supabase ist nicht konfiguriert.'};const{data,error}=await supabase.rpc('get_contact_presence',{p_conversation_id:conversationId});const first=Array.isArray(data)?data[0]:data;return{data:(first as ContactPresence|undefined)??null,error:error?.message??null}}
export async function setConversationTyping(conversationId:string,isTyping:boolean){if(!supabase)return{error:'Supabase ist nicht konfiguriert.'};const{error}=await supabase.rpc('set_conversation_typing',{p_conversation_id:conversationId,p_is_typing:isTyping});return{error:error?.message??null}}
export async function loadConversationTyping(conversationId:string){if(!supabase)return{data:false,error:'Supabase ist nicht konfiguriert.'};const{data,error}=await supabase.rpc('get_conversation_typing',{p_conversation_id:conversationId});return{data:Array.isArray(data)&&data.length>0,error:error?.message??null}}
export function subscribeToConversationRealtime(conversationId:string,handlers:{onStatus?:(status:string)=>void;onMessagesChanged?:(change:MessageRealtimeChange)=>void;onReadChanged?:()=>void;onTypingChanged?:()=>void}):RealtimeChannel|null{if(!supabase)return null;const scopedChange=(payload:unknown)=>{const change=normalizeMessageRealtimeChange(payload);handlers.onMessagesChanged?.({...change,scopeId:change.scopeId??conversationId})};let subscribed=false;return supabase.channel(`direct-conversation:${conversationId}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'direct_messages',filter:`conversation_id=eq.${conversationId}`},scopedChange).on('postgres_changes',{event:'UPDATE',schema:'public',table:'direct_messages',filter:`conversation_id=eq.${conversationId}`},scopedChange).on('postgres_changes',{event:'*',schema:'public',table:'direct_conversation_reads',filter:`conversation_id=eq.${conversationId}`},()=>handlers.onReadChanged?.()).on('postgres_changes',{event:'*',schema:'public',table:'conversation_typing',filter:`conversation_id=eq.${conversationId}`},()=>handlers.onTypingChanged?.()).subscribe(status=>{handlers.onStatus?.(status);if(status==='SUBSCRIBED'){if(subscribed)handlers.onMessagesChanged?.({event:'INSERT',messageId:null,scopeId:conversationId});subscribed=true}})}
export function subscribeToDirectMessagesRealtime(onChanged:()=>void):RealtimeChannel|null{if(!supabase)return null;return supabase.channel('direct-message-list').on('postgres_changes',{event:'INSERT',schema:'public',table:'direct_messages'},onChanged).on('postgres_changes',{event:'UPDATE',schema:'public',table:'direct_messages'},onChanged).on('postgres_changes',{event:'INSERT',schema:'public',table:'direct_conversation_reads'},onChanged).on('postgres_changes',{event:'UPDATE',schema:'public',table:'direct_conversation_reads'},onChanged).on('postgres_changes',{event:'INSERT',schema:'public',table:'direct_conversations'},onChanged).on('postgres_changes',{event:'UPDATE',schema:'public',table:'direct_conversations'},onChanged).subscribe()}
export async function unsubscribeConversationRealtime(channel:RealtimeChannel|null){if(!supabase||!channel)return;await supabase.removeChannel(channel)}
